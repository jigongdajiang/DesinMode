<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gjg.com.desinmode">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".BaseApplication">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".d02_singleton.atvmanager.InfoActvity"/>
        <activity android:name=".d02_singleton.atvmanager.LoginActivity"/>
        <activity android:name=".d02_singleton.atvmanager.RegActivity"/>
        <activity android:name=".d01_factorymethod.iofram.FactoryActivity"/>
        <activity android:name=".d07_decorator.headerrv.HeaderRvActivity"/>
        <activity android:name=".d09_adapter.smaplelv.AdapterDemoActivity"/>
    </application>

</manifest>